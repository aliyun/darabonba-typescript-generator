import Source;
import Source.Util as SourceUtil;
import Source.Util2;
import LocalSource;

type @id = [ string ];
type @str = string;


init(id: [ string ], str: string) {
    @id = id;
    @str = str;
}

static function Sample(client: Source): void {
    Util2.test1();
    var a:SourceUtil.M = SourceUtil.test1();
    var runtime = new Source.RuntimeObject{};
    var request = new LocalSource.Request{
        accesskey = "accesskey",
        region = "region",
    };
    client.print(runtime);
}
